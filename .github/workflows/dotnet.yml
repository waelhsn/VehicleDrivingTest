name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Build
        run: dotnet build ../../../*.sln
      - name: Test
        run: dotnet test --no-restore --verbosity normal
